export const APP_NAME = 'Pulse Nova';
export const APP_VERSION = '1.0.0';

// Development Mode: Set to true to skip BLE connection requirement (testing without hardware)
export const DEV_MODE = false;

// Fixed local user ID — all data is stored locally, no cloud auth
export const LOCAL_USER_ID = 'local-user';
